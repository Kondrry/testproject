# Test Project

Милипиздрический учебный проект на Python для практики Git и модулей.

## Возможности

Проект содержит функции:

- `is_even(number)` — проверяет, является ли число чётным;
- `average(numbers)` — вычисляет среднее значение списка.

## Структура проекта

```text
testproject/
├── main.py
├── helpers.py
├── README.md
└── .gitignore